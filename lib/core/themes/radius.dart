import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class AppRadius {
  static const loginInputFieldRadius = OutlineInputBorder(borderRadius: BorderRadius.all(Radius.circular(4)));
}